package com.gotour.catalog.pkg.domain;

/** Commercial category of a tour package. */
public enum PackageType {
    GROUP,
    PRIVATE,
    HONEYMOON,
    FAMILY,
    ADVENTURE,
    CRUISE,
    WEEKEND
}
