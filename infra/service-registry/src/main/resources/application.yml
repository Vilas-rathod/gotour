server:
  port: 8761

spring:
  application:
    name: service-registry

eureka:
  client:
    # The registry itself does not register or fetch
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
